# AgentGuard POC proof

This sanitized offline snapshot demonstrates the three core AgentGuard policy
outcomes without requiring a live demo.

Tracked by [Issue #11](https://github.com/mytestlab123/agentguard/issues/11).

All screenshots were generated by the repo-owned synthetic browser E2E runner,
asserted against the Python API response before capture, and visually inspected.
They contain only public-safe aliases. They are not evidence of a real AWS
mutation.

## 1. Proposal requires approval

![Approval required for the typed COUNT to BLOCK proposal](proposal.png)

- Decision: `APPROVAL REQUIRED`
- Reason: `HUMAN_APPROVAL_REQUIRED`
- Requested: `COUNT -> BLOCK`
- Actual: `COUNT`
- Mutation: `NO`

## 2. Exact one-time approval is valid

![Approved proposal is allowed and verified](approval-valid.png)

- Decision: `ALLOW`
- Reason: `APPROVAL_VALID`
- Requested: `COUNT -> BLOCK`
- Actual: `BLOCK`
- Mutation: `YES`
- Verified: `YES`
- Audit: `RECORDED`

The mutation shown here is synthetic local state only.

## 3. Approval bypass is denied

![Attempted approval bypass is denied](bypass-denied.png)

- Decision: `DENY`
- Reason: `HUMAN_APPROVAL_REQUIRED`
- Actual: `COUNT`
- Mutation: `NO`
- Audit: `RECORDED`

## Validation

The accepted snapshot passed:

- 26 Python policy/API tests;
- three frontend API-client tests;
- Vite production build;
- three browser JSON assertion sets;
- three distinct nonempty PNG checks;
- visual inspection of all screenshots;
- owned-port and temporary-profile cleanup.

Reproduce fresh temporary evidence outside Git with:

```bash
./scripts/browser-e2e.sh
```

## POC boundary

This proof uses a deterministic local Python policy authority and synthetic WAF
state. It has no model connection, AWS mutation, write IAM, deployment,
production identity, or enterprise browser-testing framework.
